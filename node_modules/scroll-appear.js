/* const offset = window.innerHeight*0.5;
const aboutSection = document.querySelector('#about');

window.addEventListener('scroll', function () {
    if(scrollY>=offset){
        console.log("FIRE")
        aboutSection.style.filter = ('opacity(1)');
    }
}) */